import { FirestoreDataConverter } from '@firebase/firestore';
export { FirebaseConfig } from './firebase.config';
export { FirestoreDataConverter };

import * as firebaseConfigs from './firebase.config';
export { firebaseConfigs };
